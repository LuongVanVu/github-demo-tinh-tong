#include <stdio.h>

int tong(int x, int y)
{
    return x + y;
}
int hieu(int x, int y)
{
    return x - y;
}

int max(int x, int y)
{
    if (x >= y)
    {
        return x;
    }
    else
        return y;
}